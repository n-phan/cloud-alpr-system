# SAM build cache — regenerated by `sam build` / `make deploy-stack`
.aws-sam/

# Deployment zips — regenerated by `make deploy fn=<name>`
lambda/**/function.zip

# Pip install staging dirs left behind if packaging is interrupted
lambda/**/package/
